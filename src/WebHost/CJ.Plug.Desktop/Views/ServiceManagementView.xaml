<UserControl x:Class="CJ.Plug.Desktop.Views.ServiceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             xmlns:views="clr-namespace:CJ.Plug.Desktop.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#1E1E2E">
    <UserControl.Resources>
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#2D2D3F"/>
            <Setter Property="Foreground" Value="#B0B0C8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="0,0,0,3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter Content="{TemplateBinding Header}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#1E1E2E"/>
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#7C7CF0"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#303050"/>
                                <Setter Property="Foreground" Value="#D0D0E8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <TabControl Background="#1E1E2E"
                BorderBrush="#35355F"
                BorderThickness="0,0,0,0">
        <!-- Tab 1: 仪表盘 -->
        <TabItem Header="仪表盘">
            <Border BorderBrush="#35355F" BorderThickness="1,1,0,0">
                <wv2:WebView2 x:Name="DashboardWebView"/>
            </Border>
        </TabItem>

        <!-- Tab 2: 控制面板 -->
        <TabItem Header="控制面板">
            <views:ServiceControlView x:Name="ControlPanelView"/>
        </TabItem>

        <!-- Tab 3: 数据库配置 -->
        <TabItem Header="数据库配置">
            <views:DatabaseConfigView x:Name="DatabaseConfigView"/>
        </TabItem>
    </TabControl>
</UserControl>
