<Page x:Class="CJ.Plug.StationSetup.Pages.CompletePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      FontFamily="Microsoft YaHei UI" FontSize="14"
      Background="White">
    <Grid Margin="40,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TxtTitle" Text="安装完成"
                   FontSize="20" FontWeight="Bold" Foreground="#1565C0"/>
        <Rectangle Grid.Row="1" Height="1" Fill="#E0E0E0" Margin="0,8,0,0"/>

        <StackPanel Grid.Row="2" Margin="0,20,0,0">
            <TextBlock x:Name="TxtMessage" TextWrapping="Wrap" Margin="0,0,0,20"/>
            <TextBlock Text="安装目录：" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBlock x:Name="TxtInstallPath" Foreground="Gray" Margin="0,0,0,16"/>

            <CheckBox x:Name="ChkLaunchSettingUI" Content="立即启动 StationSettingUI"
                      IsChecked="True" Margin="0,0,0,8"/>
        </StackPanel>
    </Grid>
</Page>
