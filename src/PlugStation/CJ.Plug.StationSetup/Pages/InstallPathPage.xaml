<Page x:Class="CJ.Plug.StationSetup.Pages.InstallPathPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      FontFamily="Microsoft YaHei UI" FontSize="14"
      Background="White">
    <Grid Margin="40,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="安装配置" FontSize="20" FontWeight="Bold" Foreground="#1565C0"/>
        <Rectangle Grid.Row="1" Height="1" Fill="#E0E0E0" Margin="0,8,0,0"/>

        <StackPanel Grid.Row="2" Margin="0,20,0,0">
            <!-- 安装路径 -->
            <TextBlock Text="安装路径：" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <DockPanel Margin="0,0,0,16">
                <Button x:Name="BtnBrowse" Content="浏览..." DockPanel.Dock="Right"
                        Width="70" Margin="8,0,0,0" Click="BtnBrowse_Click"/>
                <TextBox x:Name="TxtInstallPath" Text="{Binding InstallPath, UpdateSourceTrigger=PropertyChanged}"
                         Height="30" VerticalContentAlignment="Center" Padding="4"/>
            </DockPanel>

            <!-- 主服务器地址 -->
            <TextBlock Text="主服务器地址：" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <TextBox x:Name="TxtMainServerUrl" Text="{Binding MainServerUrl, UpdateSourceTrigger=PropertyChanged}"
                     Height="30" VerticalContentAlignment="Center" Padding="4" Margin="0,0,0,16"/>
            <TextBlock Text="请输入平台主服务器的访问地址，例如 http://192.168.1.100:8686"
                       Foreground="Gray" FontSize="12" Margin="0,0,0,20"/>

            <!-- 选项 -->
            <CheckBox Content="创建桌面快捷方式（StationSettingUI）"
                      IsChecked="{Binding CreateDesktopShortcut}" Margin="0,0,0,8"/>
            <CheckBox Content="安装完成后自动启动 StationApiServer"
                      IsChecked="{Binding AutoStartService}" Margin="0,0,0,8"/>
        </StackPanel>
    </Grid>
</Page>
