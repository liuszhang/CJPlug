<Page x:Class="CJ.Plug.StationSetup.Pages.ProgressPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      FontFamily="Microsoft YaHei UI" FontSize="14"
      Background="White">
    <Grid Margin="40,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="正在安装" FontSize="20" FontWeight="Bold" Foreground="#1565C0"/>
        <Rectangle Grid.Row="1" Height="1" Fill="#E0E0E0" Margin="0,8,0,0"/>

        <StackPanel Grid.Row="2" Margin="0,30,0,0" VerticalAlignment="Center">
            <ProgressBar x:Name="ProgressBar" Height="24" Minimum="0" Maximum="100"
                         Value="{Binding ProgressValue}" Margin="0,0,0,12"/>
            <TextBlock x:Name="TxtStatus" Text="{Binding StatusText}"
                       TextWrapping="Wrap" Foreground="#424242"/>
            <TextBlock x:Name="TxtDetail" Text="" Foreground="Gray" FontSize="12"
                       Margin="0,8,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Page>
