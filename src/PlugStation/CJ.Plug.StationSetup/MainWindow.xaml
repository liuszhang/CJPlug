<Window x:Class="CJ.Plug.StationSetup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="图站安装向导" Width="640" Height="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="Microsoft YaHei UI"
        FontSize="14">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 页面容器 -->
        <Frame x:Name="PageFrame" Grid.Row="0"
               NavigationUIVisibility="Hidden"
               JournalOwnership="OwnsJournal"/>

        <!-- 底部按钮栏 -->
        <Border Grid.Row="1" Background="#F0F0F0" Padding="12,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnBack" Content="上一步" Style="{StaticResource WizardButton}"
                        Click="BtnBack_Click" Visibility="Collapsed"/>
                <Button x:Name="BtnNext" Content="下一步" Style="{StaticResource WizardButton}"
                        Click="BtnNext_Click" IsDefault="True"/>
                <Button x:Name="BtnCancel" Content="取消" Style="{StaticResource WizardButton}"
                        Click="BtnCancel_Click" IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
